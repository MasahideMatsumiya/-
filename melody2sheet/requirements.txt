# メロディ譜の自動生成に必須
basic-pitch
music21

# PNG画像出力（--png）に使用。MuseScore不要で楽譜を画像化できる
verovio
cairosvg

# 任意: --vocals（ボーカル抽出）を使う場合のみ
# demucs

# 補足: 環境によっては pkg_resources エラーを避けるため setuptools<81 が必要な場合があります
#   pip install "setuptools<81"
